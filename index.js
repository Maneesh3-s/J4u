/**
 * @format
 */

import {AppRegistry} from 'react-native';
import LandingPage from './src/screens/phase1/landingPage';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => LandingPage);
